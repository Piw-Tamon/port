var typed = new Typed(".multiple-text", {
    strings: ["Web Development Learner Guide", "Learner", "Writer"], // ใช้ 'strings' ไม่ใช่ 'Strings'
    typeSpeed: 90,
    backSpeed: 90,
    backDelay: 900,
    loop: true
});
